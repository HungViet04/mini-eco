Frontend (Vite + React)

Quick start:

1. cd frontend
2. npm install
3. npm run dev

The frontend expects the backend API at `http://localhost:3000/api`. To change, set `VITE_API_BASE` in your environment.

Features included:
- Login / Register
- Product list
- Admin product create
- Simple cart + create order
